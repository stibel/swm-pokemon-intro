import { StyleProp, ViewStyle } from "react-native";

export const screenView: StyleProp<ViewStyle> = {
    flex: 1,
    paddingVertical: 64,
    paddingHorizontal: 32,
    gap: 16,
    overflow: 'hidden',
}